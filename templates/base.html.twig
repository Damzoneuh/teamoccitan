<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Team Occitan - {% endblock %}</title>
        <meta name="description" content="Rejoignez la team occitans, team iracing francophone, nous faisons principalement de l'endurence mais nous sommes avant tout une bande de potes qui aiment rouler ensemble .">

        <meta name="keywords" content="iracing , simu ,simracing,  simulation , team , team france , francais , porshe , ferrari , gte , occitans , occitan , esport , rebellion , redface ,wrt, pegasus, iracing.fr , endu , endurance, team occitans, team francaises iracing, iracing esport">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <script src="https://kit.fontawesome.com/a499f7b8cf.js" crossorigin="anonymous"></script>
        <link rel="shortcut icon" type="image/png" href="{{ asset('assets/logo-tor.png') }}"/>
        {% block stylesheets %}{{ encore_entry_link_tags('global') }}{% endblock %}
        <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
    </head>
    <body>
    {% if is_granted('ROLE_EDITOR') %}
    <div class="d-flex justify-content-around align-items-center">
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('admin') }}" >Admin</a>
        {% endif %}
        <a href="{{ path('editor') }}">Editeur</a>
    </div>
    {% endif %}

    {% if app.request.pathInfo == '/' %}
        <div id="banner"></div>
    {% endif %}
    <div id="nav" data-connected="{{ app.user ? '1' : '0' }}"></div>
    {% for message in app.flashes('success') %}
        <div class="container-fluid mt-5 fixed-top">
            <div class="row mt-5">
                <div class="col-12 col-md-6 offset-md-3 mt-5">
                    <div class="alert alert-success" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                        <h4 class="alert-heading">Succès</h4>
                        <hr>
                        <p class="mb-0">{{ message }}</p>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="container-fluid mt-5 fixed-top">
            <div class="row mt-5">
                <div class="col-12 col-md-6 offset-md-3 mt-5">
                    <div class="alert alert-danger" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                        <h4 class="alert-heading">Oups</h4>
                        <hr>
                        <p class="mb-0">{{ message }}</p>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
        {% block body %}

        {% endblock %}
    <div id="footer"></div>
        {% block javascripts %}
            {% if app.request.pathInfo == '/' %}
                {{ encore_entry_script_tags('banner') }}
            {% endif %}
            {{ encore_entry_script_tags('nav') }}
            {{ encore_entry_script_tags('footer') }}
        {% endblock %}
    </body>
</html>

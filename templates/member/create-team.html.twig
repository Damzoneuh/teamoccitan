{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} Admin{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col mt-2 mb-4">
                <div class="p-4 bg-blue-gradient text-grey-inherit">
                    <h1 class="mt-4 mb-4 text-center">Créer une team</h1>
                    <div class="row">
                        <div class="col-sm-12 col-md-6 offset-md-3">
                            {{ form_start(form) }}
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
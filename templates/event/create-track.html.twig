{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Admin{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col mt-4 mb-5">
                <div class="p-4 bg-blue-gradient text-grey-inherit">
                    <h1 class="mt-2 mb-4 text-center text-grey-inherit">Créer un circuit</h1>
                    {{ form_start(form) }}

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
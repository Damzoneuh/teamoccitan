{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Admin{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col mt-4 mb-4">
                <h1 class="text-center mt-4 mb-4 text-blue">Ajouter un role a {{ user.name }} {{ user.lastname }}</h1>
                <div class="p-4 bg-blue-gradient text-grey-inherit">
                    {{ form_start(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
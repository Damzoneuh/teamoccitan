{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Admin{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col">
                <div class="mt-4 mb-4 bg-blue-gradient text-grey-inherit p-lg-4 p-sm-2">
                    <h1 class="text-grey-inherit text-center mt-4 mb-4">Ajouter un skin</h1>
                    {{ form_start(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}